# Titan API Configuration
# Copy this file to .env and fill in your values

# Required: Your Titan API authentication token
TITAN_TOKEN=your-titan-api-key-here

# Optional: Custom Titan WebSocket URL (default: wss://api.titan.ag/api/v1/ws)
# TITAN_URL=wss://api.titan.ag/api/v1/ws

# Required for swap command: Path to your Solana keypair JSON file
KEYPAIR_PATH=/path/to/your/keypair.json

# Optional: Custom Solana RPC URL (default: https://api.mainnet-beta.solana.com)
# Use a private RPC for better reliability (Helius, QuickNode, etc.)
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
